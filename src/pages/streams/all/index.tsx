import { Layout } from '@/components/layouts/Layout/Layout';
import { StreamsAllView } from '@/components/views/StreamsAllView';

const StreamsAll = () => {
  return (
    <Layout>
      <StreamsAllView />
    </Layout>
  );
};

export default StreamsAll;
