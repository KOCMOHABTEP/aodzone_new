import Layout from "@layouts/Layout/Layout";

const NotificationPage = () => {
    return (
        <Layout>
            <NotificationPage />
        </Layout>
    );
};

export default NotificationPage;
