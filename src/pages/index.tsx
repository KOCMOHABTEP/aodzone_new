import { Layout } from "@/components/layouts/Layout/Layout";
import { HomeView } from "@/components/views/HomeView";

const Home = () => {
    return (
        <Layout>
            <HomeView />
        </Layout>
    );
};

export default Home;
