import { Layout } from '@/components/layouts/Layout/Layout';
import { CreateTeamView } from '@/components/views/CreateTeamView';

const CreateTeam = () => {
  return (
    <Layout>
      <CreateTeamView />
    </Layout>
  );
};
export default CreateTeam;
